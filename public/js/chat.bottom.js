// Definições para apresentação do campo de chat
let chatHeight = window.screen.height;
let chatArea = doc.TAG(dom.CHAT_PANEL);
chatArea.style.height = (chatHeight - 250) + 'px';
chatArea.style.maxHeight = (chatHeight - 250) + 'px';