/**
 * MANIPULAÇÃO DE ELEMENTOS DO MATERIALIZE CSS A TODA APLICAÇÃO
 * Inicializa os elementos do Materialize;
 */
$(document).ready(function() {

    let materialize = new MaterializeController();
    materialize.initiateMaterialize();
});